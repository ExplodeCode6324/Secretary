# ID

宿主产生的 UUID；模型不可冒充宿主或 Master 身份。

来源：[contracts.schema.json](../../src/contracts/contracts.schema.json#/$defs/ID)。此页自动生成；行为以调用模块为准。

```json
{
  "type": "string",
  "minLength": 1,
  "description": "宿主产生的 UUID；模型不可冒充宿主或 Master 身份。",
  "format": "uuid"
}
```
