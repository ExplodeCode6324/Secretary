# Digest

已保存原始字节 SHA-256 小写十六进制。

来源：[contracts.schema.json](../../src/contracts/contracts.schema.json#/$defs/Digest)。此页自动生成；行为以调用模块为准。

```json
{
  "type": "string",
  "minLength": 1,
  "description": "已保存原始字节 SHA-256 小写十六进制。",
  "pattern": "^[0-9a-f]{64}$"
}
```
