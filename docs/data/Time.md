# Time

UTC RFC3339；时区意图在 Trigger.timezone 另存。

来源：[contracts.schema.json](../../src/contracts/contracts.schema.json#/$defs/Time)。此页自动生成；行为以调用模块为准。

```json
{
  "type": "string",
  "minLength": 1,
  "description": "UTC RFC3339；时区意图在 Trigger.timezone 另存。",
  "format": "date-time",
  "pattern": "Z$"
}
```
