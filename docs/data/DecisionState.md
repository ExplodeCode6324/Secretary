# DecisionState

状态枚举对应 src/state_machine/catalog.json 的 Decision

来源：[contracts.schema.json](../../src/contracts/contracts.schema.json#/$defs/DecisionState)。此页自动生成；行为以调用模块为准。

```json
{
  "type": "string",
  "enum": [
    "OPEN",
    "ANSWERED",
    "OBSOLETE",
    "EXPIRED"
  ],
  "description": "状态枚举对应 src/state_machine/catalog.json 的 Decision"
}
```
